# v1.5.12 Animation Replacements Report

## Scope

Integrated the approved `meowmoon_codex_bundle_v4` special pin-hit animations as a focused animation release.

## Registry entries

- `millyardgearv3` — Millyard Gear Machine — 3600 ms
- `jellybeanv3` — Jellybean Row Color Pop — 3400 ms
- `campwagonv2` — Camp Wagon Roll — 3500 ms
- `flowerwavev2` — Flower Wave Bloom — 3400 ms

## Replacement handling

- Replaced the previous active Millyard gear animation entry with `millyardgearv3`.
- Replaced the previous active Jellybean row animation entry with `jellybeanv3`.
- Replaced the previous active Camp wagon animation entry with `campwagonv2`.
- Added `flowerwavev2` as a new animation.

## Extracted reusable functions

- Millyard: `millyardgearv3DrawMachine`, `millyardgearv3DrawGear`, and prefixed helper functions.
- Jellybean: `jellybeanv3Draw`, `jellybeanv3DrawHouse`, `jellybeanv3DrawStar`, and prefixed helper functions.
- Camp wagon: `campwagonv2Draw`, `campwagonv2DrawStar`, and prefixed helper functions.
- Flower wave: `flowerwavev2Draw` and prefixed helper functions.

## Integration adjustments

- Removed review-page scaffolding: canvas setup, pause/restart controls, resize logic, standalone animation loops, and DOM-only behavior.
- Kept all animation logic deterministic and elapsed-time based.
- Increased the Jellybean row horizontal clamp margin so the six-house v3 footprint reads cleanly near side pins.
- Omitted full-canvas review-page sky/ground fills from Camp Wagon v2 and Flower Wave Bloom v2 so the reusable in-game functions do not cover the bowling playfield.

## Static validation

- `node --check game/app-v1.5.12.js` passed.
- `node --check game/sw.js` passed.
- Registry count is 71.
- Function declarations with `draw` in the name: 177.
- The replaced older active registry names are absent from `app-v1.5.12.js`.

## Browser validation

A temporary local-only URL query hook was used to force each new/replacement animation once, then removed before final checks.

- `millyardgearv3`: forced playback, waited past duration, clicked again afterward, no console warnings/errors.
- `jellybeanv3`: forced playback, waited past duration, clicked again afterward, no console warnings/errors.
- `campwagonv2`: forced playback, waited past duration, clicked again afterward, no console warnings/errors.
- `flowerwavev2`: forced playback, waited past duration, clicked again afterward, no console warnings/errors.

The final app file contains no forced-test hook.
